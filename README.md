# RESTful, Api, Spring boot, JPA, MySQL

## Databaze
- mysql -u root -p
- create database restful;
- GRANT ALL PRIVILEGES ON restful.* TO 'restful'@'localhost';
- FLUSH PRIVILEGES;
- mysql -u restful -p

## Spustit
mvn clean spring-boot:run
